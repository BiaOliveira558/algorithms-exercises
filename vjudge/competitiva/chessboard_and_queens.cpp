#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define fastio ios::sync_with_stdio(false); cin.tie(nullptr)

 
int main() {
    fastio;
    
    char board[8][8];
    for (int i = 0; i < 8; i++)
        for (int j = 0; j < 8; j++)
            cin >> board[i][j];

    int col[8] = {0};
    int d1[15] = {0};
    int d2[15] = {0};

    int row = 0;
    vector<int> pos(8, -1);


    return 0;
}
