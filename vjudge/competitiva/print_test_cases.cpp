#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int i = 1;

    while (true) {
    int input;
    cin >> input;

    if (input == 0) break; 

    cout << "Case "<< i <<": "<< input << "\n";
    i++;
    }


    return 0;
}